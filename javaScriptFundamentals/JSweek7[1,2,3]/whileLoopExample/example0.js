let m = 0,
  n = 1,
  evenNumbers = [],
  oddNumbers = [];

// Use the while loop here
while (m <= 100) {
    evenNumbers.push(m);
    m += 2;
  }
  console.log(`Even Numbers (0 - 100): ${evenNumbers.toString()}`);