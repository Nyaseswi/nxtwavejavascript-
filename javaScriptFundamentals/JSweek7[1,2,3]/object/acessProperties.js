'use strict';
var user = {
    name: 'Michelle Roberts',
    age: 28
}
console.log(user.name); // This is the correct way to access the "name" property using dot notation.
console.log(user['name']); // This is the correct way to access the "name" property using bracket notation.
console.log(user.gender);