'use strict'; 

const isPalindrome = word => { 

  let wordArr = []; 

  let revWord = []; 

  for (const w of  word) { 

  wordArr.push( ); 

  revWord.push( ); 

  }

  return wordArr.toString() === revWord.reverse().toString() 

}

console.log(isPalindrome('hello') ? 'Is a palindrome!' : 'Not a palindrome...') 
// civic pop crisp 
