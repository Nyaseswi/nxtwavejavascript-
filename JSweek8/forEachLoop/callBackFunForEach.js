'use strict';
const books = [
    'The hound of baskervellis',
    'Avatar',
    'Crisp and shine',
    'The founder',
]
books.forEach(function( element,index) {
    console.log(element);
    console.log(index);
    
});